$(document).ready(function(){
    //Retorna ou define a largura de um elemento
    // console.log($(".paragrafo").width());
   
    //Retorna ou define a largura de um elemento, levando em conta o padding
    // console.log($(".paragrafo").innerWidth());
    
    //Retorna ou define a altura de um elemento
    console.log($(".paragrafo").height());

    //Retorna ou define a altura de um elemento, levando em conta o padding
    console.log($(".paragrafo").innerHeight());
    

    // |==>> POSITIONS 
    //Retornando ou definindo a posição de um elemento
    // $("form .field:eq(1)").offset({
    //     top:5,
    //     left:100
    // });

    //Retorna a posição de um elemento se baseando no elemento pai
    // $("form .field:eq(1)").position()

    //Retorna ou define a posição da barra de rolagem -> eixo y
    // $(document).scrollTop(900)

    //Retorna ou define a posição da barra de rolagem -> eixo x
    // $(document).scrollLeft(900) 

});